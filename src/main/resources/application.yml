spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xmas
    username: postgresusr
    password: 2%sZa*[g(@3D

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

flyway:
  url: jdbc:postgresql://localhost:5432/xmas
  user: postgresusr
  password: 2%sZa*[g(@3D

jwt:
  app:
    jwtSecret: xmasLotterySecretKey
    jwtExpirationMs: 86400000